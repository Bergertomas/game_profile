# Should I Play? — Deliberate-Drift Log

**Status:** Open running record  
**Owner of the format:** [Shared Design-System and Interaction Handoff v1.0
§15](Should_I_Play_Shared_Design_System_and_Interaction_Handoff_v1.0_2026-08-31.md)  
**Companion:** [Accessibility and Visual-Conformance Matrix
v1.0](Should_I_Play_Accessibility_and_Conformance_Matrix_v1.0_2026-08-31.md) §6

Every intentional departure from the accepted Fable specimen is recorded here,
with the reason and the evidence behind it. Pixel equality is not the goal;
handoff §15 and matrix §6 require that any material difference in proposition
hierarchy, section order, art-led/artless parity, Search prominence,
identity/radar relationships or type/colour/spacing roles is either corrected or
logged. This is the log.

An entry is not permission. It records a decision somebody can review, argue
with and reverse.

---

## A. Entries carried from the handoff

These are the seven the handoff itself names as already-known drift (§15). They
are recorded here so the log is complete rather than starting mid-story.

| Surface/component | Reference | Observed difference | Reason | Decision/evidence | Owner/date | Follow-up |
|---|---|---|---|---|---|---|
| Homepage opening | A1/A2 Rev 5.1, 1440 and 390 | Hero/artwork composition is shorter than the specimen | Approved refinement — Search gains prominence | ADR 0030 non-blocking owner refinement, 30 Aug 2026 | Tomas / 2026-08-30 | none |
| All surfaces | A1–A6, C1–C4 | Visible text floor is 12px; the specimen's 8.5–11px labels are not reproduced | Accessibility (WCAG 1.4.4, matrix X-05) | Handoff §2.2 rule 4; `tests/e2e/type-floor.spec.ts` measures painted text at 320/390×667/1440 | Handoff v1.0 / 2026-08-31 | none |
| All interactive components | A1–A6, C1–C4 | Hit areas are at least 44×44 CSS px even where the visible shape is smaller | Accessibility (matrix X-10) | Handoff §2.2 rule 5 | Handoff v1.0 / 2026-08-31 | none |
| Control boundaries and focus | A1–A6, C1–C4 | Boundaries and focus rings are stronger than the specimen's hairlines | Accessibility (WCAG 1.4.11, matrix X-04) | Handoff §2.2 rule 6; `border.control` / `border.focus` tokens | Handoff v1.0 / 2026-08-31 | none |
| Type system | A7 | Three functional type roles (Archivo / Newsreader / JetBrains Mono), not two | Reconciliation of A7 against the accepted screens | Handoff §2.2 rule 1 | Handoff v1.0 / 2026-08-31 | none |
| Editorial surface | A3–A6 | One warm editorial ground on an otherwise dark product | Accepted profile direction; not a second card theme | Handoff §2.2 rule 2, §3.1 | Handoff v1.0 / 2026-08-31 | none |
| Compare CTA copy | A1/A2 | "See the full comparison — artwork-free" replaced by "See the full comparison" | Content truth — Compare is art-led with artless parity (ADR 0033) | Handoff §2.2; asserted in `tests/curated-compare.test.ts` | Handoff v1.0 / 2026-08-31 | none |

---

## B. Engineering Slice 2 — the accepted homepage system

| # | Surface/component | Reference | Observed difference | Reason | Decision/evidence | Owner/date | Follow-up |
|---|---|---|---|---|---|---|---|
| B-01 | Homepage — the warm "In the library" card grid | A1/A2 Rev 5.1, 1440 and 390 | The pre-Gate-A card grid is removed; the accepted poster rail takes its place | Accepted composition. ADR 0030 and Plan §5.2 list a mosaic, a poster rail, authored shelves and a curated module; a second full catalogue listing on the same page is in none of them | ADR 0030 decision list; Plan §5.2; `app/(public)/page.tsx` | Claude / 2026-09-01 | none. `#catalogue` still resolves — the rail's heading carries it, asserted in `tests/e2e/homepage.spec.ts` |
| B-02 | Poster frame | A1/A2 poster rail | Posters use a 3:4 frame rather than the 2:3 cover frame `GameCard` uses | Responsive stress. At 320–390px a 2:3 poster inside a scrolling rail costs roughly 300px of height per row before the plate, which pushes the shelves below it out of reach on a short phone. Artwork is atmospheric here and may be cropped (ADR 0030) | `components/home/home-sections.css`; reflow assertions in `tests/e2e/homepage.spec.ts` | Claude / 2026-09-01 | revisit if a cover-art-heavy catalogue makes the crop lossy |
| B-03 | Rail track | A1/A2 poster rail, 1440 | The track bleeds past the 82rem measure at viewports wider than ~87rem while the heading, note and credits stay inside it | Faithfulness to the accepted composition: a rail clipped hard at the measure reads as a truncated grid rather than as something that scrolls | `components/home/home-sections.css` | Claude / 2026-09-01 | none |
| B-04 | Authored shelves | A1/A2 shelves | No shelf renders on production today | Content truth. The objective shelves that are configured resolve to the entire three-profile catalogue, which is not a selection, and every evergreen/living collection is a qualitative editorial claim Tomas approves (P0.3). Padding the design with an invented collection is the failure this rule exists to prevent | `content/home-shelves.ts`; `lib/home/shelves.ts`; `tests/home-shelves.test.ts`; states reviewable at `/dev/home-states` | Claude / 2026-09-01 | shelves appear as soon as an approved collection or a catalogue larger than one shelf exists. No code change needed |
| B-05 | "Choosing between…" | A1/A2 curated module | The module renders nothing on production today | Content truth. A pairing and its tension sentence are both owner-approved editorial claims and none exists | `content/curated-compare.ts`; `tests/curated-compare.test.ts`; state reviewable at `/dev/home-states` | Claude / 2026-09-01 | as B-04 |
| B-06 | "Choosing between…" CTA | A1/A2; handoff §7.3 | The accepted "See the full comparison" route is not published; the module states that full Compare is not built instead | Technical dependency. `/compare` is Slice 4. Publishing the route would be a broken link; printing the label as inert text would imply a destination that does not exist | `components/home/CuratedCompare.tsx` takes the route as a parameter; both branches asserted in `tests/curated-compare.test.ts` | Claude / 2026-09-01 | Slice 4 passes `compareRouteFor` and the accepted CTA appears with no component change |
| B-07 | Homepage journey labels | Handoff §5.2 | Search / Compare / What should I play? are a marked list, not an ARIA tablist | The handoff's tablist contract is conditional on the three labels switching one colocated content region. They do not: Compare and What should I play? are accepted and unbuilt, so there is no panel to switch and a tab that responded to Enter by doing nothing would teach a visitor the product is a mock-up | Carried from Slice 1 (`components/home/HomeOpening.tsx`); handoff §5.2 first sentence | Claude / 2026-09-01 (carried) | becomes a tablist when a second journey has a panel |
| B-08 | Poster preview panel | Handoff §7.1 | The preview carries the one-line experience, primary pull and primary risk rather than the eight exact values | Handoff §7.1 makes a compact fingerprint decorative when the eight exact values are available in "the expanded preview **or** the linked profile destination". The poster links to the profile, which carries all eight; eight long dimension names inside a 15.5rem poster would wrap to roughly forty lines and bury the editorial fields a reader actually wants at this size | `components/home/ProfilePoster.tsx`; `tests/home-rail.test.ts` | Claude / 2026-09-01 | revisit if the poster ever loses its profile link |
| B-09 | Homepage artwork | A1/A2, all viewports | Every poster and mosaic tile renders the artless typographic sleeve | Rights. Production clears no artwork (ADR 0011), and evaluation-basis assets may never reach production (ADR 0012, `npm run check:containment`) | `lib/profile/artwork.ts`; art-led parity asserted against a cleared fixture in `tests/home-rail.test.ts` | Carried from ADR 0011 | none. Art-led and artless are the same component and neither outranks the other |
| B-10 | Public header | A1/A2 chrome, 390 at 200% text | The one compact chrome row is allowed to wrap onto a second line, and only at extreme zoom (see F-08: after Compare joined the row it wrapped at 390 at normal text until the mobile disclosure was added) | Responsive stress. At 390 CSS px with the root font doubled, the wordmark, the Search opener and "How we score" cannot share a line, and a header that refuses to wrap scrolled the whole document sideways on every page — which handoff §3.4 forbids | `components/SiteChrome.tsx`; 200% assertions in `tests/e2e/homepage.spec.ts` | Claude / 2026-09-01 | none. Nothing wraps at any ordinary size |

## C. Defects found and fixed while implementing Slice 2

Recorded here rather than in the drift table because they are corrections, not
departures. Both were latent before this slice and both were found by the
conformance envelope the matrix requires.

| # | Where | Defect | Fix |
|---|---|---|---|
| C-01 | `components/home/home-opening.css`, `components/home/home-sections.css` | The artwork rights notice carries the URL an asset was loaded from. A URL is one unbreakable token, so it set a min-content floor wider than a 320px phone and scrolled the document sideways — a preview-only credit line becoming a WCAG 1.4.10 failure | `overflow-wrap: anywhere` on both notices |
| C-02 | `components/home/home-sections.css` | Visually-hidden text is `position: absolute`, and an absolutely positioned box whose containing block is outside a scroll container is not clipped by it. A poster's screen-reader label three screens along the rail was laid out against the viewport and pushed the document sideways | `position: relative` on the poster and on each control that carries a hidden label |

## D. Engineering Slice 3 — the accepted profile system

Reference frames are A3 (art-led desktop, 1440), A4 (art-led mobile, 390),
A5 (artless desktop) and A6 (artless mobile). The production state today is A5/A6
on every profile, because no artwork is cleared (ADR 0011); A3/A4 are reviewable
on any non-production build through the evaluation-clearance overlay.

| # | Surface/component | Reference | Observed difference | Reason | Decision/evidence | Owner/date | Follow-up |
|---|---|---|---|---|---|---|---|
| D-01 | Practical commitment | A3–A6 fit/commitment block | No total-commitment or useful-session facts render on any profile | Content truth. The specimen values ("Substantial", "45–90 minutes", "Needs room to breathe") are layout material (ADR 0032). No approved scope-aware record exists, and handoff §4.3 permits omission where the contract allows absence; a row of `Unknown`s would imply a record was consulted | `lib/profile/practical.ts`; `DecisionBand.tsx` renders the band only from a record; states at `/dev/profile-states`; `tests/practical-presentation.test.ts` | Claude / 2026-09-01 | the band appears when the first approved record is passed in. No code change needed |
| D-02 | Where to play | A3–A6 destinations layer | No storefront action renders | Content truth. No verified destination record exists (ADR 0029); the contract in `lib/commerce/storefront.ts` hides an unverified action rather than manufacturing one | `tests/profile-composition.test.ts` asserts no destination and no `compare` link | Claude / 2026-09-01 | Phase 4's verified official action layer |
| D-03 | Compare with | A3–A6 exit | No editor-selected "Compare with" entry point renders | Technical dependency and content truth. `/compare` is Slice 4 and no editor-selected pair exists (`content/curated-compare.ts` is empty) | as D-02 | Claude / 2026-09-01 | Slice 4 |
| D-04 | Status line | A3–A6 "Evaluated through" | The date is labelled "Evidence cut-off", never "Evaluated" | Content truth. The public meaning and source field of "Evaluated" is an open decision (Master Plan §17.3, ADR 0032); the record's own term is used until it is resolved | `IdentityStage.tsx`; asserted absent in both suites | Claude / 2026-09-01 | resolve the label/source decision, then rename in one place |
| D-05 | Platform identity | A3–A6 platform logos | Full platform names as square text tags; no logos | Content truth. No approved platform-mark asset exists (brief §13); the accepted screens refuse abbreviations and the names are the accessible identity either way | `IdentityStage.tsx` `PlatformList` | Claude / 2026-09-01 | approved marks sit beside the names as decoration; the names stay |
| D-06 | Platform warning | A3 desktop order; brief §9 | The evaluation-level warning sits directly under the one-line answer rather than beside the scope line or after the instrument | Responsive stress and decision truth. In the scope block it pushed the answer below the first viewport at 390×667; after the instrument it would be read after the decision it qualifies (ADR 0032). The itemised notes and overrides follow the instrument as the brief's item 8 | `DecisionBand.tsx`; first-viewport assertion in `tests/e2e/profile-conformance.spec.ts` | Claude / 2026-09-01 | none |
| D-07 | Mobile radar | A4/A6 instrument, 390 and 320 | Below the 40rem content breakpoint the radar is a 15rem decorative overview with no axis labels; the eight exact rows immediately follow | Accessibility. Eight two-line names at the 12px floor do not fit around a chart inside a 320px phone, and shrinking them is what the contract forbids; handoff §9.2 specifies exactly this fallback | `profile.css`; `tests/e2e/profile-conformance.spec.ts` "decorative overview on a phone" | Claude / 2026-09-01 | revisit if a mobile label geometry is accepted |
| D-08 | Desktop radar labels | A3/A5 instrument | Axis labels are HTML text placed around the SVG, two lines per name at 0.75rem with the value at 0.875rem, rather than SVG text | Accessibility. SVG text scales with the viewBox and cannot hold the 12px floor at 200% text; HTML labels grow with the root font | `ProfileInstrument.tsx`; 200% reflow and label-size assertions in `tests/e2e/profile-conformance.spec.ts` | Claude / 2026-09-01 | none |
| D-09 | Exact row disclosure | A3–A6 exact rows | "Why this score?" is an explicit 44px button beside the reading, not the whole row | Accessibility. A whole-row trigger gives a screen reader the entire reading — name, gloss, value, confidence — as one control's name; an explicit trigger keeps the reading as text and carries the dimension in its own accessible name (matrix X-15) | `instrument.tsx` | Claude / 2026-09-01 | none |
| D-10 | Exact row disclosure | D3 lineage | Rows open independently; opening one no longer closes another | Accessibility. Brief §7.2: opening one row does not reorder or trap focus; a keyboard reader comparing two dimensions should not lose the first | `instrument.tsx` | Claude / 2026-09-01 | none |
| D-11 | Concise fit guidance | A3–A6 fit summary + detailed fit | The three governed interpretation blocks render once, in the concise position; no separate "detailed fit" section | Content truth. The record carries three blocks and nothing shorter; a one-line fit summary would be new editorial content nobody approved. Experience traits carry the detailed position | `DecisionBand.tsx`, `ProfileDetail.tsx` | Claude / 2026-09-01 | none |
| D-12 | Identity stage | A3 art-led desktop ~431px; A5 artless ~374px | Art-led header ≈ 24–25rem with the plate; the art strip on a phone is 8rem; the artless plate is the typographic field with no reserved height | Responsive stress and handoff §2.1/§8.1. The title, scope and answer must be on screen at 390×667 and the answer with Pull/Tax at 1440×900; reference heights are conformance references, not fixed | `profile.css`; first-viewport assertions | Claude / 2026-09-01 | none |
| D-13 | Identity stage | Handoff §4.2 | The accent wash of the artless field is painted under the artwork in the art-led state too | Artwork state model. A slow, blocked or failed image then shows the authored field rather than an empty black rectangle; the same move Slice 2 made for posters | `profile.css` `.gp-identity` | Claude / 2026-09-01 | none |
| D-14 | Exit | A3–A6 "more profiles" | The exit is the accepted poster rail ("More profiles"), not the warm `GameCard` grid | Faithfulness to the accepted system: one catalogue grammar across the homepage and the profile, on the dark ground. `GameCard` remains the card grammar and is exercised by `/dev/radar-states` | `ProfilePage.tsx` | Claude / 2026-09-01 | retire `GameCard` if no surface adopts it |
| D-15 | Client boundary | ADR 0032 implementation boundary | `GameProfile` is a server component; `ProfileInstrument` is the one client leaf | Bundle impact, measured: profile HTML 161,279 → 150,049 bytes, page-specific client chunk 32,102 → 25,280 bytes, RSC payload 72,312 → 75,574 bytes. Hover linking and disclosure behaviour preserved | `GameProfile.tsx`, `ProfileInstrument.tsx` | Claude / 2026-09-01 | none |
| D-16 | Provisional caveat | Matrix P-09 | A one-sentence caveat follows the status line on a Provisional profile | Accessibility. Words and caveat, not colour alone; the sentence paraphrases Rubric §13 ("released, but incomplete/conflicting evidence or rapid change") and adds no claim | `IdentityStage.tsx` `ScopeStatus` | Claude / 2026-09-01 | owner may reword |
| D-17 | Corrections | Brief §6.1 trust band | No correction route or address is offered | Content truth. corrections@ is not operational (Master Plan P0.10); the accepted trust sentence is used without its "report a correction" clause | `TrustBand.tsx` | Claude / 2026-09-01 | P0.10 |

---

## E. Engineering Slice 4 — the accepted full Compare system

Reference frames are C1 (art-led desktop, 1440), C2 (art-led mobile, 390),
C3 (artless desktop) and C4 (artless mobile), with the C-rail states. The
production state today is C3/C4 on every pair, because no artwork is cleared
(ADR 0011); C1/C2 and the mixed states are reviewable on any non-production
build through the evaluation-clearance overlay and at `/dev/compare-states`.

| # | Surface/component | Reference | Observed difference | Reason | Decision/evidence | Owner/date | Follow-up |
|---|---|---|---|---|---|---|---|
| E-01 | Eligibility | C-rail selector; Master Plan §5.4 | Only published primary profiles can be chosen; sibling scopes (DLC, expansion, mode) are offered as ineligible rows and refused by address | Owner decision. The accepted address names each side by game slug; encoding a scope would invent a contract | ADR 0033 amendment, 2 Sept 2026; `lib/compare/index.ts`, `selection.ts`; multi-scope Playwright project | Tomas / 2026-09-02 | sibling-scope Compare is a later decision |
| E-02 | Editorial trade-off | C1/C2 opening sentence | The first prose is the relation summary in dimension names ("Alan Wake 2 is higher on…; they are equal on…"), not an authored pair sentence | Content truth. No editor has approved a sentence about any pair (P0.3; ADR 0034 illustrative copy is provisional); the summary is derived from the fixed relation rule and nothing else | `RelationField.tsx`; `tests/compare-composition.test.ts` | Claude / 2026-09-02 | an approved pair sentence can be added as configuration when one exists |
| E-03 | Pair address | ADR 0033; ADR 0017 | A pair is the prerendered launcher document plus a client-restored selection; `noindex, follow` is a query-conditioned response header repeated in the document's robots meta after hydration | Technical limit. The Worker has no corpus at request time and no pair is prerendered, so the distinction cannot live in server-rendered HTML | `next.config.ts` headers; `CompareApp.tsx`; `tests/compare-metadata.test.ts`; `cf:verify` asks workerd | Claude / 2026-09-02 | a per-profile static document is the scaling path (P2) |
| E-04 | Launcher | ADR 0033 "indexable only with substantive guidance" | The launcher is indexable and in the sitemap, carrying what a comparison shows, the relation rule, eligibility, every eligible game with links, and sharing | Approved refinement within the ADR's condition | `LauncherGuidance.tsx`; `app/sitemap.ts` | Claude / 2026-09-02 | none |
| E-05 | Paired radar | C1 ~340px labelled seam radar | No axis labels on the paired radar at any size; the eight paired rows are the precision equivalent | Accessibility and handoff §9.2: eight labels over two scrims cannot hold the floor at the seam; the rows immediately follow and the legend names both shapes | `PairedRadar.tsx`; `CompareStage.tsx` legend | Claude / 2026-09-02 | revisit if a labelled seam geometry is accepted |
| E-06 | Paired radar | C2 ~128px | The seam radar is decorative (`aria-hidden`) at every size; both identities are named in text and in the legend's marker grammar | Handoff §9.2 | `CompareStage.tsx` | Claude / 2026-09-02 | none |
| E-07 | Controls | C1/C2 Replace beside each identity | Replace left, Replace right and Copy link sit after the paired rows, in DOM and on screen | Handoff §10.6 fixes the focus order (identity actions … row disclosures, left Replace, right Replace, Copy link) and forbids visual repositioning that reorders focus | `CompareView.tsx`; keyboard-order assertion in `tests/e2e/compare.spec.ts` | Claude / 2026-09-02 | none |
| E-08 | Paired row | C1 dumbbell row | DOM order is name, left side, right side, relation; on a wide screen the relation is placed between the sides by grid area | Matrix C-09 reading order; the relation holds no control, so focus order is unaffected | `PairedInstrument.tsx`; `compare.css` | Claude / 2026-09-02 | none |
| E-09 | Identity stage | C1 art fields; C3 territories | Art fields are 24rem desktop / 12rem mobile with artwork and 12rem / 8rem without; in a mixed state both fields take the art-led height | Handoff §4.2: equal territories in every state; no reserved hole when neither has art | `compare.css` `.cp-art`; `tests/e2e/compare.spec.ts` "equal identity order" | Claude / 2026-09-02 | none |
| E-10 | Identity stage | Handoff §4.2 | The accent wash and the game's own outline fragment are painted under the artwork in every state | Artwork state model: loading, failed and absent resolve to the authored territory, never an empty frame or a broken-image glyph | `CompareStage.tsx` `ArtField`; failed and slow-load tests | Claude / 2026-09-02 | none |
| E-11 | Legend | C1 | The shape legend sits under the radar in the seam column on a wide screen and after both identities on a phone; the DOM places it after both identities at every width | Reading order (matrix C-03) | `compare.css` `.cp-legend` | Claude / 2026-09-02 | none |
| E-12 | Selector | C-rail selector | Recognised-unprofiled titles and sibling scopes appear as `aria-disabled` rows with the reason, rather than being hidden | Content truth: the site knows them; answering "we do not recognise that title" would be false | `SelectorDialog.tsx`; recognized-registry and multi-scope Playwright projects | Claude / 2026-09-02 | none |
| E-13 | Copy link | C-rail | The copied address is this page on this host, in the selected order; it does not claim canonical status and a preview link stays a preview link | Handoff §10.6: the action "does not claim the pair page is canonical/indexable" | `CompareControls.tsx` | Claude / 2026-09-02 | none |
| E-14 | Row disclosure | C1 per-row disclosure | "Details" shows the dimension's question, each side's Range/Not scored explanation and platform notes/overrides, and a link to each Game Profile's own rationale; subcriterion rationales are not repeated | Bounded scope: the profile owns the rationale (ADR 0032); Compare states the relation | `PairedInstrument.tsx` | Claude / 2026-09-02 | none |
| E-15 | Practical time, destinations | C1/C2 practical band, "Where to play" | Neither renders on Compare | Content truth, as D-01 and D-02 | `CompareStage.tsx` | Claude / 2026-09-02 | as D-01/D-02 |
| E-16 | Profile "Compare with" | Master Plan §5.4, A3–A6 exit | Still absent from the profile | Content truth: no editor-selected pair exists; D-03's technical dependency is resolved, the editorial one is not | `content/curated-compare.ts` remains empty | Claude / 2026-09-02 | owner-approved pairs |
| E-17 | Homepage journeys and chrome | A1/A2 journey labels; handoff §5.1 | "Compare" in the opening is a link to `/compare` and "Compare" joins the header navigation; "What should I play?" stays text | Contract activation now the destination exists (Master Plan §5.2: Compare immediately available, never the default subject) | `HomeOpening.tsx`; `SiteChrome.tsx`; `tests/e2e/search.spec.ts` | Claude / 2026-09-02 | none |
| E-18 | Mobile opening | C2 at 390×667 "both titles and the trade-off before the first scroll" | The first viewport holds the `h1` naming both games, the lede ("differences and trade-offs, never a winner"), the diptych and the seam radar; the written relation summary follows the two identities | Reading order (matrix C-03, C-09): both identities are named in words before analytical depth, and the identities carry scope, evidence and the platform warning that the summary depends on | `tests/e2e/compare.spec.ts` "keeps the accepted order" | Claude / 2026-09-02 | revisit if the owner prefers the summary above the identities on a phone |

---

## F. Homepage implementation-fidelity pass — 2 September 2026

Reference frames are A1 (desktop, 1440) and A2 (mobile, 390) Rev 5.1 as frozen
by ADR 0030, read directly from the Fable **Should I Play - Canonical
Screens.dc.html** artifact (Claude Design project "Artifact review ready",
`data-screen-label="A1 Homepage desktop"` / `"A2 Homepage 390"`) and measured
against handoff v1.0 §§2.1, 3.4, 5.2, 7.1. The rendered page was measured
rather than assumed. Specimen facts — commitment bands, dates, roster, artwork,
the "14 profiles at launch" footnote, the "Profiles"/"About" navigation — are
not publication truth and were not copied.

| # | Surface/component | Reference | Observed difference | Reason | Decision/evidence | Owner/date | Follow-up |
|---|---|---|---|---|---|---|---|
| F-01 | Opening, structure | A1/A2 hero + console | The opening is now the accepted two-part composition: a hero (proposition beside a fixed-width mosaic on desktop; proposition above it on the phone) followed by a full-width console panel carrying the journey pills and the Search field. Slices 1–2 had set Search and the journey labels inside the left hero column, with the mosaic beside them | Faithfulness. The accepted A1/A2 place Search in a panel under the hero on both frames; ADR 0030 "without an introductory scroll" and the handoff's "header + hero + at least 12rem of the Search region" arithmetic describe exactly that order | `components/home/HomeOpening.tsx`; `home-opening.css`; `tests/e2e/homepage.spec.ts` "puts the console after the hero in the focus order" | Claude / 2026-09-02 | none |
| F-02 | Opening, journey switcher | A1/A2 `role="tablist"` pills; handoff §5.2 | Search / Compare / What should I play? are pills in the console — Search filled, Compare outlined and a real link, What should I play? quiet text without a boundary — with the Search field directly beneath as the selected panel. Still a marked list, not an ARIA tablist (B-07 carried) | Faithfulness to the accepted pill grammar; B-07's reason still holds (no colocated region is switched, so a tab that did nothing would misdescribe the product). The unbuilt journey is deliberately not drawn as a control | `HomeOpening.tsx` `Journeys`; `tests/e2e/search.spec.ts` "leads with the question"; `homepage.spec.ts` "puts the Search field directly under a switcher that has Search selected" | Claude / 2026-09-02 | becomes a tablist when a second journey has a panel |
| F-03 | Opening, Search action | A1/A2 cyan "Search" action in the well | The inline field carries the accepted cyan Search button. It does what Enter does — opens the highlighted row, else the query's one exact match — and otherwise returns focus to the field; it never navigates on a guess. Its accessible name is "Search the guide" so the header's "Search" opener stays unique | Faithfulness; ADR 0031 (no navigation on a guess) | `components/search/SearchField.tsx`; `search.css` `.sip-search__submit` | Claude / 2026-09-02 | none |
| F-04 | Opening, mosaic geometry | A1 560×420, `1.15fr 1fr` × `1fr 1fr`; A2 390-wide × 250 | The same geometry at every width: one game spanning both rows of the left column, two stacked beside it. Desktop height is 360px rather than 420 | ADR 0030 owner refinement (section A): the hero is modestly shorter so Search gains prominence; 360 keeps the hero, with its caption, inside the handoff's ~27.5rem | `home-opening.css` `.sip-open__tiles`; `homepage.spec.ts` "keeps the accepted mosaic geometry" and "keeps the desktop hero inside its 27.5rem maximum" | Claude / 2026-09-02 | none |
| F-05 | Opening, fingerprints | A1 148px lead / 96px small; A2 76px / 46px; handoff §7.1 "72px desktop and 64px mobile targets" | Integrated in the plate beside the words, as the specimen composes them: 120px lead / 96px small on desktop, 76px / 46px on the phone | The accepted composition ("hero radars integrated into their tiles and substantially larger", Rev 5 changelog) is owned by ADR 0030; the handoff's 72/64 targets describe the compact rail level, which the poster mark keeps. The lead fingerprint is trimmed from 148 to 120 so the published one-line experience — three times the specimen's length — keeps a readable column. All are decorative (`aria-hidden`); the shape is stated in text in every tile | `home-opening.css` `.sip-open__fingerprint` | Claude / 2026-09-02 | owner may prefer the full 148px lead fingerprint |
| F-06 | Opening, tile copy | A1/A2 tile plates | Title, one-line experience (lead tile, desktop only) and evidence status; no commitment band; the "Game Profile · 8 dimensions · 0–10" cue is carried once by the caption under the mosaic | Content truth (no approved commitment record — ADR 0032, handoff §4.3) and responsive stress: the published one-liners filled a 190px phone tile. The sentence is one click away and in the rail's preview | `HomeOpening.tsx` `Tile`; `tests/e2e/search.spec.ts` "shows three profiles, each with a text equivalent of its shape" | Claude / 2026-09-02 | none |
| F-07 | Opening and poster rail, artless territory | A1/A2 sleeves (spine + uppercase title); handoff §4.2, §8.1; E-10 | Every artless tile and poster paints the game's own eight-axis outline faint and large across its accent wash (`ShapeFragment`), under any artwork, with the fingerprint in its corner; the specimen's tiny uppercase sleeve title is not reproduced (the title is set once, in the plate) | Art-led/artless parity with the grammar the Compare territory already uses, so one game is recognisably itself in all three places; a second title inside the frame at 10px would break the 12px floor | `components/home/ShapeFragment.tsx`; `.sip-open__fragment`; `.sip-poster__fragment` | Claude / 2026-09-02 | none. B-09 (no cleared artwork) is unchanged |
| F-08 | Mosaic and poster artwork | Handoff §4.2 "where the adjacent title already names the game, artwork uses empty alt text" | `alt=""` on every mosaic tile and poster image | Contract, and a defect: with a non-empty alt, Chromium paints a broken-image glyph over the territory when review-clearance art fails to load — the "no broken-image state" the matrix forbids | `HomeOpening.tsx`; `ProfilePoster.tsx`; `tests/home-rail.test.ts`; `homepage.spec.ts` "paints no broken-image glyph" | Claude / 2026-09-02 | none |
| F-09 | Poster rail | A1 152px 2:3 posters, round 44px controls flanking the track; A2 150px posters, swipe only | Posters are 150/152px at 2:3 with the control radius (supersedes B-02's 3:4 15.5rem frame); the step controls are round and flank the track from the content breakpoint, and follow the track on a phone rather than disappearing | Faithfulness; handoff §7.2 still requires labelled 44px previous/next actions, so they are kept on the phone where A2 relies on swipe alone | `components/home/PosterRail.tsx`; `home-sections.css` `.sip-rail__body`, `.sip-poster` | Claude / 2026-09-02 | none |
| F-10 | Poster preview control | A1 hover/focus overlay; handoff §7.2 explicit disclosure; B-08 | "Preview" remains an explicit disclosure button under the poster (B-08 carried), now sentence-case Archivo at the small size rather than a 12px uppercase key | Handoff §7.2 and brief §6 ("avoid uppercase everywhere"); the control keeps its 44px target, boundary and `aria-expanded` state | `home-sections.css` `.sip-poster__disclose` | Claude / 2026-09-02 | none |
| F-11 | Public chrome | A1 header ~63px, A2 ~54px; handoff §5.1 | One compact row at both widths on the contract's chrome surface. Below the content breakpoint the row is wordmark + Search opener + one **Menu** disclosure holding Compare and How we score; above it the links are in the row. The specimen's "Profiles", "Method" and "About" links are not reproduced: no all-profiles or About route exists, and "How we score" is the product's chosen label | Conformance and content truth. Since E-17 added Compare, the row had wrapped to two lines (98px) on every 390px phone at normal text, which B-10 had said happened only at 200%; the legacy graphite read as a lighter band above the canvas | `components/SiteChrome.tsx`; `components/ChromeNav.tsx`; `components/site-chrome.css`; `homepage.spec.ts` "the public chrome on a phone" | Claude / 2026-09-02 | supersedes B-10's "nothing wraps at any ordinary size" — true again; the row still wraps at 390 with 200% text, as B-10 allows |
| F-12 | Wordmark | A1–A6, C1–C4 wordmark: Archivo 700, normal width, sentence case, coral "?" | The header and footer set the wordmark as every accepted screen does. The condensed uppercase logotype came from the D3/ADR 0013 system, which ADR 0030 superseded | Faithfulness to the accepted screens. Scoped to the chrome; the `sip-wordmark` class and the share card are unchanged | `site-chrome.css` `.sip-chrome__wordmark, .sip-foot__wordmark` | Claude / 2026-09-02 | owner may confirm the wordmark treatment for the share card too |
| F-13 | Opening, Search label | Handoff §6.2 "persistent visible label"; A1/A2 filled "Search" pill | In the console the field's own `<label>` is visually hidden and the filled Search pill is the region's visible name; the label still gives the combobox its accessible name | Faithfulness: the accepted console names the field with the selected pill, and printing "Search" twice a line apart read as an error. The header dialog keeps its visible label | `home-opening.css` `.sip-open__search .sip-search__label` | Claude / 2026-09-02 | none |
| F-14 | "What you are looking at" explainer | A1/A2 have no methodology explainer; A1 ends with an "Every profile in the guide" A–Z panel | The explainer is retained (Master Plan §5.1 lists "the explanation" in the implemented route) but moved onto the contract tokens, with the labelled SVG radar replaced by the label-free profile geometry and the eight exact values as an HTML list. The A–Z panel is not added: with three profiles it would repeat the rail (B-01) | ADR 0030 safeguard ("genuinely readable labels or an aligned exact-value list"; SVG text fell below the 12px floor at 320px); scope | `app/(public)/page.tsx` `ProfileExplainer`; `home-sections.css` `.sip-explainer*` | Claude / 2026-09-02 | owner may remove the explainer or add the A–Z panel when the catalogue outgrows the rail |
| F-15 | Opening and explainer layout | Handoff §3.4 "content-first"; "at 200% zoom, all pages become a single reading column where necessary" | The two-column compositions switch on by container width measured in `em`, not by viewport media queries | Responsive stress. A viewport query cannot see a 200% root text size; at 1280px with the root doubled the wide grid overflowed the document sideways (WCAG 1.4.10). Container queries in the reader's own text size give the single column the handoff asks for | `home-opening.css` `container-type`, `@container (min-width: 60em)`; `home-sections.css`; 200% assertions in `homepage.spec.ts` | Claude / 2026-09-02 | none |
| F-16 | Opening, preview rights notice | ADR 0012; A1 "Private design-only" badge | On a preview build the rights notice for review-clearance mosaic art renders after the console, not under the mosaic | So an accountability line never displaces the accepted composition or pushes Search out of the 390×667 viewport; production carries no notice | `HomeOpening.tsx` | Claude / 2026-09-02 | none |
| F-17 | Document ground | Handoff §2.2 "a dark public ground with one deliberate warm editorial surface" | `body` still paints the legacy warm paper (`--color-page`); every homepage band paints the canvas itself, so nothing light shows on the homepage | Scope. The methodology page still reads on that light ground and is outside this homepage pass | `app/globals.css` compatibility layer | Claude / 2026-09-02 | migrate `body` to `color.surface.canvas` when the methodology page moves onto the contract |
| F-18 | Authored shelves and "Choosing between…" | A1 shelf grammars (16:10 grid, list cards, 16:9 cards) and duel cards | Unchanged in this pass: both regions still render nothing (B-04, B-05) and the shelf component still reuses the poster rail grammar rather than A1's per-shelf card grammars | Content truth and scope: no approved collection or pair exists, so the accepted grammars have nothing to render against | `content/home-shelves.ts`; `content/curated-compare.ts` | Claude / 2026-09-02 | implement A1's shelf and duel card grammars when the first approved collection or pair exists |
