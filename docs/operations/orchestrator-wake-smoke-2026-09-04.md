# Orchestrator wake smoke fixture

Temporary non-production fixture for the post-merge event-wake smoke test on 2026-09-04.

This branch/PR exists only to cause an ordinary CI completion so `.github/workflows/orchestrator-wake.yml` can prove that it emits exactly one metadata-only wake comment from current `main`. Do not merge this fixture. Close the PR and delete the branch after the smoke evidence is captured.
